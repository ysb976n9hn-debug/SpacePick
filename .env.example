# Optional. When set, SpacePick calls OpenAI Images edits for photoreal redesigns.
# Without this key the app runs fully offline in Demo mode (canvas compositing).
OPENAI_API_KEY=

# Optional overrides (live mode only)
# OPENAI_IMAGE_MODEL=gpt-image-1
# OPENAI_IMAGE_QUALITY=medium
