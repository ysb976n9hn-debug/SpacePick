# Required for Live AI (photoreal OpenAI image edits).
# Without this key, SpacePick uses a labeled DEMO — not real AI.
OPENAI_API_KEY=

# Optional overrides (live mode only)
# OPENAI_IMAGE_MODEL=gpt-image-2
# OPENAI_IMAGE_QUALITY=high
